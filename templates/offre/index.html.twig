{% extends 'template.html.twig' %}

{% block title %}Hello StageController!{% endblock %}

{% block body %}


<div class="container-fluid" >
<div class="card shadow mb-4">
    <div class="card-header py-3">
<div class="card-body table-responsive">
<div class="container" >
   <form action="" enctype="multipart/form-data" method="POST" role="form" class="php-email-form" style="width:700px" >
     <div class="row">
                                  <table class="table table-bordered table-striped table-highlight">
                                <a href="{{ path('add_offre') }}" class="btn btn-success mb-2">Nouvelle publication</a>
                                                    <thead>
                                                        <tr>
                                                          
                                                        <th>Nombre</th>
                                                        <th>Domaine</th>
                                                        <th>Objet</th>
                                                        <th>Date de debut</th>
                                                        <th>Date de fin</th>
                                                        <th>Action 1</th>
                                                        <th>Action 2</th>
                                                        <th>Action 3</th>
                                                        </tr>
                                                    </thead>
                                                    <tbody>
                                                        {% for offre in offres %}
                                                        <tr>
                                                            
                                                        <td>{{offre.nombre}}</td>
                                                        <td> {{offre.domaine}} </td>
                                                        <td> {{offre.objet}} </td>
                                                        <td> {{offre.dateDebut|date("d/m/Y")}} </td>
                                                        <td> {{offre.dateFin|date("d/m/Y")}} </td>
                                                        <td>
                                                        <a href="{{ path('offre_show', {id: offre.id}   )}}" class="btn btn-primary">Détails</a>
                                                        </td>
                                                        <td>
                                                        <a href="{{ path('offre_delete', {id : offre.id}   )}}  " class="btn btn-danger" >REMOVE</a>
                                                        </td>
                                                        <td>
                                                        <a href="{{ path('offre_edit', {id : offre.id}   )}}  " class="btn btn-success" >MODIFIER</a>
                                                        </td>
                                                    </tr>
                                                    {% endfor %}
                                                            
                                                        
                                                    </tbody>
                                                </table>
                                            
                                        </div>
                                    </form>



                                






{% endblock %}

